MikiLab v84 — Il forno si accende (segreto del 16 ottobre) + Il banco delle prove + Il mio forno
Da applicare DOPO la v83 (già applicata: verificato nello zip _57_). Solo frontend, nessuna modifica al backend né al database.

1) IL SEGRETO DEL 16 OTTOBRE (Giornata mondiale del pane) — frontend/src/components/LancioSegreto.jsx
   Conto alla rovescia nascosto: nessun link visibile nel sito. Si apre in due modi:
   - toccando 8 volte di seguito il polpo nel footer (8 come i tentacoli);
   - con l'indirizzo segreto mikilab.de/?forno=1 (da usare come teaser su TikTok).
   Chi lo trova vede un puntino ambra accanto al polpo e da lì lo riapre con un tocco.
   Mostra: forno con l'impasto che "lievita" man mano che si avvicina il 16 ottobre, giorni/ore/minuti/secondi,
   "Ricordamelo" (crea un file .ics nel browser, nessun server), "Passa il segreto" (condivisione o copia link).
   Il 16 ottobre: fiocchi di farina + "Oggi il forno è acceso" e una striscia chiudibile in Home (solo quel giorno).
   Dal 17 ottobre: "Il forno è acceso dal 16 ottobre 2026" con i giorni trascorsi. Testi IT/DE/EN.

2) IL BANCO DELLE PROVE — frontend/src/components/BancoProve.jsx (in Strumenti)
   Prova del dito (impasto da toccare sullo schermo), prova della finestra, prova del galleggiamento: l'utente dice
   cosa vede e riceve verdetto + cosa fare, con "Chiedi a Sitor" precompilato. Più "Il tempo di oggi": gradi in cucina
   + tempo della ricetta → stima del tempo reale (regola ~doppio ogni +9 °C, dichiarata come stima).
   Nessun dato salvato né inviato. Le dosi delle ricette non vengono mai toccate.

3) IL MIO FORNO — frontend/src/components/MioForno.jsx (in Strumenti)
   Quaderno personale dei propri pani: foto (ridotta nel browser a 900 px), voto a stelle, nota, link alla ricetta.
   Vive SOLO nel dispositivo (IndexedDB, chiave "mioforno"). Esporta (.json locale), cancella una prova o tutto.
   "Chiedi a Sitor come migliorare" precompila la chat con nome/voto/nota (nessuna foto inviata).

File toccati:
   NUOVI: src/components/LancioSegreto.jsx, BancoProve.jsx, MioForno.jsx
   MODIFICATI: src/App.js (rotte "banco" e "mioforno", overlay segreto, banner del 16), src/components/Strumenti.jsx
   (2 voci nuove), src/components/PolpoFooter.jsx (onTap + puntino), public/sw.js (cache v64).
   LEGAL_DATA_MAP.md: aggiunta sezione 13 (strumenti locali v84).
   Eliminata la cartella capacitor-plugins/ (era nella lista della v83 ma è rimasta).

Controlli fatti da Claude: sintassi di tutti i file verificata; nessun import rotto; nessuna chiamata nuova al server;
nessun cookie, nessun tracciamento, nessun servizio esterno. Ricette, Sitor, backend e database NON toccati.
