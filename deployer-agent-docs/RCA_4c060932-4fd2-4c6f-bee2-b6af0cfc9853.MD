# RCA — Deploy run 4c060932-4fd2-4c6f-bee2-b6af0cfc9853 (edit-33)

## Outcome
Build failed at step `build_image` (Cloud Build `docker-build`). A failed run is never promoted — the app remains live on the previous good run `4e21ec6e-...` at https://edit-33.emergent.host. This deploy did NOT take the app down.

## Failed step
`build_image` → `backend-build` stage → `pip install --no-cache-dir --no-dependencies -r /app/backend/requirements.txt` (all 3 retry attempts failed).

## Root cause (source-level — NOT a manifest issue)
`backend/requirements.txt` pins a **non-existent package**:

```
line 61: lintiq==0.1.3
```

Cloud Build error (repeated on every retry):
```
ERROR: Could not find a version that satisfies the requirement lintiq==0.1.3 (from versions: none)
ERROR: No matching distribution found for lintiq==0.1.3
```

Every other requirement resolved ("Requirement already satisfied") — `lintiq` is the only failing line. It exists on **no index** (PyPI, the emergentintegrations CDN, or Artifact Registry).

## Evidence (verified in eph sandbox against this run's pulled source)
- `grep -n lintiq backend/requirements.txt` → `61:lintiq==0.1.3`
- `grep -rn lintiq backend --include='*.py'` → **no import of `lintiq` anywhere in the backend code** — it is an unused/phantom dependency.
- `pip install --dry-run 'lintiq==0.1.3'` → `No matching distribution found` (reproduces the exact build error).
- Removing line 61 leaves 0 remaining `lintiq` references. (A full re-install of the patched file in the sandbox was OOM-killed — exit 137 — due to the eph pod's memory ceiling, not a package error; Cloud Build has ample memory and already reported all other packages satisfied.)

## Fix (apply in the source environment)
Delete line 61 from `backend/requirements.txt`:

```diff
- lintiq==0.1.3
```

It is safe to remove — nothing in the codebase imports it. If the package was intended (e.g. a typo for a real library), correct the name/version instead; otherwise just remove it.

## Next step
1. E1: remove `lintiq==0.1.3` from `backend/requirements.txt` in the preview/source environment.
2. Re-trigger the deploy. The build should then pass `backend-build` and proceed through the rest of the pipeline.

Note: I did NOT re-run `resume_pipeline` — redeploying the unchanged source would fail identically. The source fix must land first.
