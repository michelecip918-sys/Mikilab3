// MikiLab è 100% gratuito: nessun paywall, nessun abbonamento.
// Questo componente resta come semplice wrapper trasparente per compatibilità.
export default function PaywallGate({ children }) {
  return <>{children}</>;
}
